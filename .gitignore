# OS / editor
.DS_Store
Thumbs.db
*.log
# Secretos / entorno — NUNCA versionar (recordar incidente .env logistika)
.env
config.php
*.local.php
# Base de datos (dev/prod) — nunca versionar: puede traer usuarios/hashes/PII
db/*.sqlite
db/*.sqlite3
db/*.db
# temporales
/tmp/
# Fase 2 (portal)
portal/lib/config.php
db/*.sqlite
